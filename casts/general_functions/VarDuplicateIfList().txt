[#name: "VarDuplicateIfList(theVar)", #scriptType: #movie, #type: #script]
on VarDuplicateIfList(theVar)
  -- returns a duplicate of the variable if it is a list
  
  if ilk(theVar, #list) then
    return theVar.duplicate()
  else
    return theVar
  end if
end






























































