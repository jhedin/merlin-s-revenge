[#name: "VarKeepInRange (var,rangest,rangefin)", #scriptType: #movie, #type: #script]
on VarKeepInRange var,rangest,rangefin
  if var> rangefin then var = rangefin
  if var< rangest then var = rangest
  return var
end VarKeepInRange































































