[#name: "CounterOnce(theCounter)", #scriptType: #movie, #type: #script]
on CounterOnce(theC)
  -- iterates a counter until fin = true; then stops.
  if theC.fin = true then
    return
  else
    Counter(theC)
  end if
end































































