[#name: "PointDirPoint(fromPoint, toPoint)", #scriptType: #movie, #type: #script]
on PointDirPoint(fromPoint, toPoint)
  dirX = VarMoreLess(fromPoint[1], toPoint[1])
  dirY = VarMoreLess(fromPoint[2], toPoint[2])
  
  dir = point(dirX, dirY)
  
  return dir
end






























































