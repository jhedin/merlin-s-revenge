[#name: "soundEmptyChan()", #scriptType: #movie, #type: #script]
on SoundEmptyChan
  -- returns an empty sound channel
  -- channel 1 is reserved for music
  
  -------------------------------------------
  --empt = 0
  --return empt
  --repeat with chan = 2 to 8
  --  if not soundbusy(chan) then
  --    empt = chan
  --    exit repeat
  --  end if
  --end repeat
  if not soundbusy(2) then
    return 2
  else if not soundbusy(3) then
    return 3
  else if not soundbusy(4) then
    return 4
  else if not soundbusy(5) then
    return 5
  else if not soundbusy(6) then
    return 6
  else if not soundbusy(7) then
    return 7
  else if not soundbusy(8) then 
    return 8   
  end if  
  --return empt
  return 0  
end SoundEmptyChan





