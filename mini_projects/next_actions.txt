mrOpen - save and load scripts
	save
		[d] create casts folder
		[d] only create relevant folders
		[d] add .txt to the end of the file name for windowses
		[d] save all members in cast
		[d] don't save bitmaps
		[d] overwrite any existing
		[d] don't save palette
		[d] files not saving properly
			CastApplyRegpoints
			CastAdjustRegpoints
			CastBulkRename
			CollisionCalcVect
			ImageDrawRepeated
			ListGetPosByProp
			PointArrivedAtTarget
			PointArrivedNearestToTarget
			PointOvershotTarget
			StringAddChars
			VarChangeInRange
			
			[d] clip filePaths to 127 chars
		[d] Still not saving properly
			ListFindString *** empty anyway ***
		
	load
		[d] erase all script members from script casts
			[d] only field and scripts
		[d] load script
		[d] create member in relevant cast
		[d] set relevant properties
			[d] scriptText
			[d] scriptType
			
		[nd] deal with ones with spaces in the name *** spaces weren't the prob***
			[d] adding quotes doesn't work
			[d] try single quotes
			[d] try escaping the space
			
		[d] Clip file lengths to 127 chars
		
		[d] load before play each time
		[d] display percent loaded on screen
			[d] remove decimal places
			[d] update every 1%
			[d] create field in temp
			[d] display in sprite 1
			[d] clean up at end
			
		[d] take notice of info line only
		
		[d] if info line is missing display errors
			create field in temp
			display in sprite 2
			clean up at end
			
		[d] resave members with shorter filenames
		
		[d] on load 
			if file contents not found
			count filePath chars
			if >127 then
				display message about moving the movie
				closer to drive root
				
		[d] make text look like mr text
		
		for master_objects
			save
				create master_objects_order.txt file in casts
				populate with list of expected filenames
					for members in master cast
				format for humans
				
			load
				read master_objects_order.txt file
				convert to an array
				loop through loading files
			
	prepare movie
		make eraseScripts()
			
			
mrOpen - release
	modWeaponManager
